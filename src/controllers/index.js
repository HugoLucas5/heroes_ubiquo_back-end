const AlignmentController = require('./alignmentsController');
const GenderController = require('./genderController');
const PublisherController = require('./publishersController');
const HeroController = require('./heroesController');

module.exports = {
    AlignmentController,
    GenderController,
    PublisherController,
    HeroController,
};